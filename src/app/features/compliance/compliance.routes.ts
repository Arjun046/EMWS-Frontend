import { Routes } from '@angular/router';
import { CompliancePageComponent } from './compliance-page.component';

export const COMPLIANCE_ROUTES: Routes = [
  { path: '', component: CompliancePageComponent }
];
