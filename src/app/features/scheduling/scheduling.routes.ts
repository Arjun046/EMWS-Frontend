import { Routes } from '@angular/router';
import { SchedulingPageComponent } from './scheduling-page.component';

export const SCHEDULING_ROUTES: Routes = [
  { path: '', component: SchedulingPageComponent }
];
