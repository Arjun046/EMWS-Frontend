import { Routes } from '@angular/router';
import { TeamCalendarPageComponent } from './team-calendar-page.component';

export const TEAM_CALENDAR_ROUTES: Routes = [
  { path: '', component: TeamCalendarPageComponent }
];
