import { Routes } from '@angular/router';
import { HelpdeskPageComponent } from './helpdesk-page.component';

export const HELPDESK_ROUTES: Routes = [
  { path: '', component: HelpdeskPageComponent }
];
