import { Routes } from '@angular/router';
import { PerformancePageComponent } from './performance-page.component';

export const PERFORMANCE_ROUTES: Routes = [
  { path: '', component: PerformancePageComponent }
];
